/** @format */

import { Box, styled } from "@mui/material";

export const SuccessMsg = styled(Box)({
  textAlign: "center",
  color: "green",
});

export const ErrorMsg = styled(Box)({
  textAlign: "center",
  color: "red",
});
